---

- name: Create and configure ansible-firefox
  hosts: client

  # TODO local testing only
  # debugger: on_failed

  roles:
    - role: ansible-firefox
      tasks_from: main
      firefox_state: present

...
